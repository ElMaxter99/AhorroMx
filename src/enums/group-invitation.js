'use strict';

const STATUS = {
  PENDING: 'PENDING',
  ACCEPTED: 'ACCEPTED',
  DECLINED: 'DECLINED',
  CANCELLED: 'CANCELLED',
  EXPIRED: 'EXPIRED',
  DRAFT: 'DRAFT',
  DELETED: 'DELETED'
};

const INVITATION_ACTION_STATUS = {
  SEND_INVITATION: 'SEND_INVITATION',
  ACCEPT_INVITATION: 'ACCEPT_INVITATION',
  DECLINE_INVITATION: 'DECLINE_INVITATION'
};

const STATUS_TREE = {
  DRAFT: ['DELETED', 'PENDING', 'CANCELLED', 'EXPIRED'],
  PENDING: ['ACCEPTED', 'DECLINED', 'CANCELLED', 'EXPIRED'],
  ACCEPTED: ['DELETED'],
  DECLINED: ['DELETED'],
  CANCELLED: ['DELETED'],
  EXPIRED: ['DELETED']
};

module.exports = {
  STATUS,
  STATUS_TREE,
  INVITATION_ACTION_STATUS
};
